name: Publish Whitehack

on:
  workflow_dispatch:
    inputs:
      tag:
        description: Annotated Whitehack release tag to publish
        required: true
        type: string

permissions: {}

concurrency:
  group: publish-whitehack-${{ inputs.tag }}
  cancel-in-progress: false

jobs:
  publish:
    name: Publish exact Whitehack release artifact to npm
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    environment: npm-bootstrap
    permissions:
      contents: write
      id-token: write
    steps:
      - name: Check out annotated release tag
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          ref: ${{ inputs.tag }}
          fetch-depth: 0
          persist-credentials: false
      - name: Set up Node 24 and npm registry
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org
      - name: Pin provenance-capable npm
        run: npm install --global --ignore-scripts npm@11.17.0
      - name: Validate release identity and main ancestry
        shell: bash
        env:
          RELEASE_TAG: ${{ inputs.tag }}
        run: |
          set -euo pipefail
          tag="$RELEASE_TAG"
          test "$GITHUB_REPOSITORY" = 'cambridgetcg/whitehack'
          readarray -t package_identity < <(node --input-type=module --eval '
            import { readFileSync } from "node:fs";
            const packageJson = JSON.parse(readFileSync("package.json", "utf8"));
            console.log(packageJson.name);
            console.log(packageJson.version);
          ')
          test "${package_identity[0]}" = 'whitehack'
          test "${package_identity[1]}" = '0.7.0'
          version="${package_identity[1]}"
          expected_tag="whitehack-v${version}"
          test "$tag" = "$expected_tag"
          test "$(git cat-file -t "refs/tags/$tag")" = tag
          tag_commit="$(git rev-list -n 1 "refs/tags/$tag")"
          test "$(git rev-parse HEAD)" = "$tag_commit"
          git fetch --no-tags origin +refs/heads/main:refs/remotes/origin/main
          git merge-base --is-ancestor "$tag_commit" refs/remotes/origin/main
      - name: Install locked validation dependencies
        run: npm ci --ignore-scripts --no-audit --no-fund
      - name: Gate the package
        run: npm test --ignore-scripts
      - name: Verify exact artifact identity and source ancestry
        shell: bash
        run: |
          set -euo pipefail
          version="$(node --input-type=module --eval "import p from './package.json' with { type: 'json' }; process.stdout.write(p.version)")"
          root="packages/v1/whitehack/${version}"
          manifest="${root}/manifest.json"
          artifact="${root}/whitehack-${version}.tgz"
          readarray -t identity < <(node --input-type=module --eval '
            import { readFileSync } from "node:fs";
            const manifest = JSON.parse(readFileSync(process.argv[1], "utf8"));
            for (const value of [
              manifest.name,
              manifest.version,
              manifest.artifact.filename,
              manifest.artifact.size,
              manifest.artifact.sha256,
              manifest.source.revision,
              manifest.source.path,
            ]) console.log(value);
          ' "$manifest")
          test "${identity[0]}" = 'whitehack'
          test "${identity[1]}" = "$version"
          test "${identity[2]}" = "whitehack-${version}.tgz"
          test "${identity[3]}" = "$(stat --format=%s "$artifact")"
          test "${identity[4]}" = "$(sha256sum "$artifact" | cut -d ' ' -f 1)"
          test "${identity[6]}" = '.'
          git cat-file -e "${identity[5]}^{commit}"
          git merge-base --is-ancestor "${identity[5]}" HEAD
          git diff --quiet "${identity[5]}" HEAD -- \
            package.json package-lock.json bin/whitehack.js src schema README.md LICENSE

          repack_dir="$(mktemp -d)"
          trap 'rm -rf "$repack_dir"' EXIT
          repacked="$(npm pack --ignore-scripts --pack-destination "$repack_dir" --silent)"
          test "$repacked" = "whitehack-${version}.tgz"
          cmp --silent "$artifact" "$repack_dir/$repacked"
      - name: Refuse an already-published immutable version
        shell: bash
        run: |
          set -euo pipefail
          version="$(node --input-type=module --eval "import p from './package.json' with { type: 'json' }; process.stdout.write(p.version)")"
          status="$(curl --silent --show-error --output /dev/null --write-out '%{http_code}' \
            --proto '=https' --tlsv1.2 --connect-timeout 10 --max-time 30 \
            "https://registry.npmjs.org/whitehack/${version}")"
          case "$status" in
            404) ;;
            200)
              echo "whitehack@${version} already exists; refusing to republish immutable bytes." >&2
              exit 1
              ;;
            *)
              echo "npm registry returned HTTP ${status}; refusing to infer package absence." >&2
              exit 1
              ;;
          esac
      - name: Publish or verify exact GitHub release mirror
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: ${{ inputs.tag }}
        run: |
          set -euo pipefail
          tag="$RELEASE_TAG"
          version="$(node --input-type=module --eval "import p from './package.json' with { type: 'json' }; process.stdout.write(p.version)")"
          filename="whitehack-${version}.tgz"
          artifact="packages/v1/whitehack/${version}/${filename}"
          release_status="$(curl --silent --show-error --output /dev/null --write-out '%{http_code}' \
            --proto '=https' --tlsv1.2 --connect-timeout 10 --max-time 30 \
            --header 'Accept: application/vnd.github+json' \
            --header "Authorization: Bearer ${GH_TOKEN}" \
            --header 'X-GitHub-Api-Version: 2022-11-28' \
            "https://api.github.com/repos/${GITHUB_REPOSITORY}/releases/tags/${tag}")"
          case "$release_status" in
            200)
              asset_count="$(gh release view "$tag" --json assets --jq ".assets | map(select(.name == \"$filename\")) | length")"
              case "$asset_count" in
                0)
                  gh release upload "$tag" "$artifact"
                  ;;
                1)
                  mirror_dir="$(mktemp -d)"
                  trap 'rm -rf "$mirror_dir"' EXIT
                  gh release download "$tag" --pattern "$filename" --dir "$mirror_dir"
                  cmp --silent "$artifact" "$mirror_dir/$filename"
                  ;;
                *)
                  echo "GitHub release $tag has ambiguous $filename assets; refusing mutation." >&2
                  exit 1
                  ;;
              esac
              ;;
            404)
              gh release create "$tag" "$artifact#$filename" \
                --verify-tag \
                --title "whitehack@${version}" \
                --notes "Exact release artifact mirror for whitehack@${version}."
              ;;
            *)
              echo "GitHub API returned HTTP ${release_status}; refusing to infer release absence." >&2
              exit 1
              ;;
          esac
      - name: Publish exact release artifact with provenance
        shell: bash
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          set -euo pipefail
          version="$(node --input-type=module --eval "import p from './package.json' with { type: 'json' }; process.stdout.write(p.version)")"
          artifact="packages/v1/whitehack/${version}/whitehack-${version}.tgz"
          npm publish "$artifact" --access public --provenance --ignore-scripts
          for attempt in $(seq 1 90); do
            if published="$(npm --userconfig=/dev/null --registry=https://registry.npmjs.org --prefer-online view "whitehack@${version}" version 2>/dev/null)" && [ "$published" = "$version" ]; then
              break
            fi
            if [ "$attempt" = 90 ]; then
              echo "npm accepted whitehack@${version}, but the public registry did not expose it within 450 seconds." >&2
              exit 1
            fi
            sleep 5
          done
