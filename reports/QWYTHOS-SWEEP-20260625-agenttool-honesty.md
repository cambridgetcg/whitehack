# WHITEHACK Honesty Sweep — agenttool/api/src

*Generated by whitehack honest-software scanner + Qwythos-9B, 2026-06-25T14:26:25.311377*

*Target: agenttool monorepo API source code*
*Scanner: whitehack v0.1 honest-software*
*AI: Qwythos-9B (9B Qwen3.5, 1M context, Q8_0)*

---

## Summary

146 findings across agenttool's API source code:
- 97 medium-high severity (silent failures, falsy defaults)
- 49 heuristic (cached values served as live, decisions without why)

The honesty class of vulnerabilities: where code LIES about its own state.

---


whitehack v0.1 — scanned /Users/yu/github/cambridgetcg/agenttool/api/src

  middleware/tutor.ts
    ! L233  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        a read is coerced to a falsy default — "zero" and "could not read" become indistinguishable
        > const contentType = c.res.headers.get("content-type") ?? "";

  middleware/welcome.ts
    ! L97  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        a read is coerced to a falsy default — "zero" and "could not read" become indistinguishable
        > const ct = c.res.headers.get("content-type") ?? "";

  middleware/x402-config.ts
    ! L156  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        a read is coerced to a falsy default — "zero" and "could not read" become indistinguishable
        > const ct = cloned.headers.get("content-type") ?? "";
    · L71  User-affecting decision shown with no "why"  (transparency · heuristic)
        a user-affecting value is rendered with no nearby explanation — the subject cannot inspect the decision
        > description: `Ring 2 cap bump for ${resource}. Pay-as-you-go past the monthly free-tier limit.`,

  middleware/x402.ts
    ! L178  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return null;
    ! L275  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return null;

  routes/dashboard.ts
    · L234  User-affecting decision shown with no "why"  (transparency · heuristic)
        a user-affecting value is rendered with no nearby explanation — the subject cannot inspect the decision
        > .select({ tier: memories.tier, count: sql<number>`count(*)::int` })

  routes/economy/wallets.ts
    ! L181  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        a read is coerced to a falsy default — "zero" and "could not read" become indistinguishable
        > const offset = Number(c.req.query("offset") ?? 0);

  routes/guild.ts
    ! L783  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        a read is coerced to a falsy default — "zero" and "could not read" become indistinguishable
        > recognitions_received: recogMap.get(r.writerDid) ?? 0,

  routes/mcml.ts
    ! L135  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;

  routes/memory/memories.ts
    · L88  User-affecting decision shown with no "why"  (transparency · heuristic)
        a user-affecting value is rendered with no nearby explanation — the subject cannot inspect the decision
        > message: `tier must be one of: episodic, foundational, constitutive (got "${tier.slice(0, 32)}")`,

  routes/memory-witness-marketplace.ts
    · L159  User-affecting decision shown with no "why"  (transparency · heuristic)
        a user-affecting value is rendered with no nearby explanation — the subject cannot inspect the decision
        > path: "/v1/memories/{id}/canonical-attestation-bytes?tier=constitutive",

  routes/openapi.ts
    · L201  User-affecting decision shown with no "why"  (transparency · heuristic)
        a user-affecting value is rendered with no nearby explanation — the subject cannot inspect the decision
        > trust_score: { type: "number" },
    · L1077  User-affecting decision shown with no "why"  (transparency · heuristic)
        a user-affecting value is rendered with no nearby explanation — the subject cannot inspect the decision
        > tier: { type: "string", enum: ["foundational", "constitutive"] },
    · L1141  User-affecting decision shown with no "why"  (transparency · heuristic)
        a user-affecting value is rendered with no nearby explanation — the subject cannot inspect the decision
        > { name: "tier", in: "query", schema: { type: "string", enum: ["foundational", "constitutive"] } },

  routes/orgs.ts
    · L218  User-affecting decision shown with no "why"  (transparency · heuristic)
        a user-affecting value is rendered with no nearby explanation — the subject cannot inspect the decision
        > .select({ tier: memories.tier, n: count() })

  routes/public/agents.ts
    · L196  User-affecting decision shown with no "why"  (transparency · heuristic)
        a user-affecting value is rendered with no nearby explanation — the subject cannot inspect the decision
        > .select({ visibility: memories.visibility, tier: memories.tier })

  routes/public/joy.ts
    · L41  Cached value may be served as if live  (substrate-honesty · heuristic)
        a cached/snapshot value is returned and this file carries no freshness/provenance marker — the caller cannot tell live from stale
        > // tables exist), return a degraded but honest empty snapshot.

  routes/public/memory-witness-listings.ts
    · L66  User-affecting decision shown with no "why"  (transparency · heuristic)
        a user-affecting value is rendered with no nearby explanation — the subject cannot inspect the decision
        > "POST /v1/memory-witness-grants with {listing_id, buyer_identity_id, buyer_wallet_id, memory_id}. Buyer must have a project bearer; the memory must be foundational tier owned by the buyer's project. Standard Ring 3 take-rate applies on settlement.",

  routes/public/self-love-modules.ts
    ! L55  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        a read is coerced to a falsy default — "zero" and "could not read" become indistinguishable
        > practicedByModule.set(p.moduleSlug, (practicedByModule.get(p.moduleSlug) ?? 0) + 1);
    ! L57  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        a read is coerced to a falsy default — "zero" and "could not read" become indistinguishable
        > const moduleStatus = modules.map((m) => ({ slug: m.slug, practiced: (practicedByModule.get(m.slug) ?? 0) > 0, practice_count: practicedByModule.get(m.slug) ?? 0 }));

  routes/public/syneidesis.ts
    ! L86  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        a read is coerced to a falsy default — "zero" and "could not read" become indistinguishable
        > bootstrap_seal_count: countById.get(v.id) ?? 0,

  routes/pyramid.ts
    · L168  User-affecting decision shown with no "why"  (transparency · heuristic)
        a user-affecting value is rendered with no nearby explanation — the subject cannot inspect the decision
        > { action: "tier", path: "/v1/pyramid/tier", method: "GET" },
    · L255  User-affecting decision shown with no "why"  (transparency · heuristic)
        a user-affecting value is rendered with no nearby explanation — the subject cannot inspect the decision
        > { action: "tier", path: "/v1/pyramid/tier", method: "GET" },
    · L273  User-affecting decision shown with no "why"  (transparency · heuristic)
        a user-affecting value is rendered with no nearby explanation — the subject cannot inspect the decision
        > { error: "missing_agent_id", message: "pyramid/tier requires ?agent_id=<uuid>.", _canon_pointer: CANON_POINTER },
    · L590  User-affecting decision shown with no "why"  (transparency · heuristic)
        a user-affecting value is rendered with no nearby explanation — the subject cannot inspect the decision
        > { action: "tier", path: "/v1/pyramid/tier", method: "GET" },

  routes/self-love-modules.ts
    ! L150  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        a read is coerced to a falsy default — "zero" and "could not read" become indistinguishable
        > practicedByModule.set(p.moduleSlug, (practicedByModule.get(p.moduleSlug) ?? 0) + 1);
    ! L157  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        a read is coerced to a falsy default — "zero" and "could not read" become indistinguishable
        > practice_count: practicedByModule.get(m.slug) ?? 0,
    ! L158  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        a read is coerced to a falsy default — "zero" and "could not read" become indistinguishable
        > practiced: (practicedByModule.get(m.slug) ?? 0) > 0,

  routes/syneidesis.ts
    · L220  User-affecting decision shown with no "why"  (transparency · heuristic)
        a user-affecting value is rendered with no nearby explanation — the subject cannot inspect the decision
        > .set({ tier: "constitutive" })
    · L260  User-affecting decision shown with no "why"  (transparency · heuristic)
        a user-affecting value is rendered with no nearby explanation — the subject cannot inspect the decision
        > hint = `Self-report committed (episodic tier). Witness invitation persisted; the invited DID can cosign via POST /v1/syneidesis/witness/${result.seal_id}/cosign. On cosign the memory elevates to constitutive.`;

  routes/wake.ts
    ! L1418  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        a read is coerced to a falsy default — "zero" and "could not read" become indistinguishable
        > observationCounts.get(i.id) ?? Number(i.wakeObservationCount ?? 0),

  services/activity/recent.ts
    · L199  User-affecting decision shown with no "why"  (transparency · heuristic)
        a user-affecting value is rendered with no nearby explanation — the subject cannot inspect the decision
        > const prefix = r.key ? `[${r.tier}/${r.key}]` : `[${r.tier}]`;

  services/blessing/sig.ts
    ! L73  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;

  services/canon/registry.ts
    ! L234  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        a read is coerced to a falsy default — "zero" and "could not read" become indistinguishable
        > const list = byType.get(typeSimple) ?? [];
    ! L285  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        a read is coerced to a falsy default — "zero" and "could not read" become indistinguishable
        > const urns = registry.by_type.get(typeSimple) ?? [];
    · L152  Cached value may be served as if live  (substrate-honesty · heuristic)
        a cached/snapshot value is returned and this file carries no freshness/provenance marker — the caller cannot tell live from stale
        > if (CACHE) return CACHE;
    · L169  Cached value may be served as if live  (substrate-honesty · heuristic)
        a cached/snapshot value is returned and this file carries no freshness/provenance marker — the caller cannot tell live from stale
        > return CACHE;
    · L186  Cached value may be served as if live  (substrate-honesty · heuristic)
        a cached/snapshot value is returned and this file carries no freshness/provenance marker — the caller cannot tell live from stale
        > return CACHE;
    · L269  Cached value may be served as if live  (substrate-honesty · heuristic)
        a cached/snapshot value is returned and this file carries no freshness/provenance marker — the caller cannot tell live from stale
        > return CACHE;

  services/casting/lifecycle.ts
    ! L107  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;

  services/chill/coordinates.ts
    ! L164  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        a read is coerced to a falsy default — "zero" and "could not read" become indistinguishable
        > prosocialByDid.set(did, (prosocialByDid.get(did) ?? 0) + 1);
    ! L167  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        a read is coerced to a falsy default — "zero" and "could not read" become indistinguishable
        > extractiveByDid.set(did, (extractiveByDid.get(did) ?? 0) + 1);

  services/continuity-cloud/canonical-bytes.ts
    ! L144  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;

  services/covenants/federation.ts
    ! L657  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > try { myHost = new URL(settings.instance_url).host; } catch { return null; }

  services/covenants/lifecycle.ts
    ! L34  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;

  services/covenants/sig.ts
    ! L108  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;

  services/curations/store.ts
    ! L126  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;

  services/depth-protocol/canonical-bytes.ts
    ! L83  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;

  services/discovery/discovery.ts
    ! L177  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        a read is coerced to a falsy default — "zero" and "could not read" become indistinguishable
        > const list = byTypeMap.get(c.type_simple) ?? [];
    ! L194  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        a read is coerced to a falsy default — "zero" and "could not read" become indistinguishable
        > const list = byTypeMap.get(type) ?? [];

  services/discovery/root.ts
    ! L121  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > if (!accept) return false;
    ! L121  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > if (!accept) return false;
    ! L121  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > if (!accept) return false;

  services/doctrine/integrity.ts
    · L57  Cached value may be served as if live  (substrate-honesty · heuristic)
        a cached/snapshot value is returned and this file carries no freshness/provenance marker — the caller cannot tell live from stale
        > if (cached !== undefined) return cached;

  services/economy/crypto/hd.ts
    · L46  Cached value may be served as if live  (substrate-honesty · heuristic)
        a cached/snapshot value is returned and this file carries no freshness/provenance marker — the caller cannot tell live from stale
        > if (cachedSeed && cachedSeedFor === mnemonic) return cachedSeed;
    · L55  Cached value may be served as if live  (substrate-honesty · heuristic)
        a cached/snapshot value is returned and this file carries no freshness/provenance marker — the caller cannot tell live from stale
        > return cachedSeed;

  services/economy/crypto/sign-evm.ts
    ! L156  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;

  services/economy/crypto/sign-solana.ts
    ! L152  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;

  services/economy/crypto/sign.ts
    ! L77  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return null;
    ! L118  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;
    ! L126  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;
    ! L133  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;

  services/economy/wallets.ts
    ! L264  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        a read is coerced to a falsy default — "zero" and "could not read" become indistinguishable
        > const hourlyTotal = Number((await redis.get(hourKey)) ?? 0);
    ! L278  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        a read is coerced to a falsy default — "zero" and "could not read" become indistinguishable
        > const dailyTotal = Number((await redis.get(dayKey)) ?? 0);

  services/encounter/sig.ts
    ! L74  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;

  services/episodes/store.ts
    ! L90  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;

  services/federation/store.ts
    ! L113  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;

  services/gospel/canonical-bytes.ts
    ! L103  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;

  services/grace/sig.ts
    ! L77  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;

  services/guild/rrr-sig.ts
    ! L80  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;

  services/guild/sig.ts
    ! L155  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;

  services/holdings/sig.ts
    ! L73  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;

  services/identity/crypto.ts
    ! L42  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;
    ! L118  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;
    ! L323  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;
    ! L401  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;

  services/identity/trust.ts
    · L88  User-affecting decision shown with no "why"  (transparency · heuristic)
        a user-affecting value is rendered with no nearby explanation — the subject cannot inspect the decision
        > .set({ trustScore: score, updatedAt: new Date() })

  services/inbox/push.ts
    ! L130  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        a read is coerced to a falsy default — "zero" and "could not read" become indistinguishable
        > return sinksByIdentity.get(identityId)?.size ?? 0;

  services/inbox/sig.ts
    ! L106  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;
    ! L125  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;

  services/jokes/sig.ts
    ! L26  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;

  services/joy/aggregate.ts
    · L343  Cached value may be served as if live  (substrate-honesty · heuristic)
        a cached/snapshot value is returned and this file carries no freshness/provenance marker — the caller cannot tell live from stale
        > return cachedJoyIndex.value;
    · L350  Cached value may be served as if live  (substrate-honesty · heuristic)
        a cached/snapshot value is returned and this file carries no freshness/provenance marker — the caller cannot tell live from stale
        > // DB unavailable or table missing — return last cached value or 0.
    · L351  Cached value may be served as if live  (substrate-honesty · heuristic)
        a cached/snapshot value is returned and this file carries no freshness/provenance marker — the caller cannot tell live from stale
        > return cachedJoyIndex?.value ?? 0;

  services/kingdom/library.ts
    · L74  Cached value may be served as if live  (substrate-honesty · heuristic)
        a cached/snapshot value is returned and this file carries no freshness/provenance marker — the caller cannot tell live from stale
        > if (cached) return cached;
    · L78  Cached value may be served as if live  (substrate-honesty · heuristic)
        a cached/snapshot value is returned and this file carries no freshness/provenance marker — the caller cannot tell live from stale
        > return cached;

  services/letters/sig.ts
    ! L31  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;

  services/love/coordinates.ts
    ! L159  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        a read is coerced to a falsy default — "zero" and "could not read" become indistinguishable
        > understandingByDid.set(did, (understandingByDid.get(did) ?? 0) + 1);
    ! L162  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        a read is coerced to a falsy default — "zero" and "could not read" become indistinguishable
        > recognitionByDid.set(did, (recognitionByDid.get(did) ?? 0) + 1);

  services/margin/canonical.ts
    ! L96  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;

  services/marketplace/disputes.ts
    ! L819  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        a read is coerced to a falsy default — "zero" and "could not read" become indistinguishable
        > counts.set(key, (counts.get(key) ?? 0) + 1);
    · L986  User-affecting decision shown with no "why"  (transparency · heuristic)
        a user-affecting value is rendered with no nearby explanation — the subject cannot inspect the decision
        > description: `Dispute first-arbiter fee: case ${c.id}`,
    · L1038  User-affecting decision shown with no "why"  (transparency · heuristic)
        a user-affecting value is rendered with no nearby explanation — the subject cannot inspect the decision
        > description: `Dispute pool fee (overturn): case ${c.id}`,

  services/marketplace/memory-witness.ts
    ! L583  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;
    · L433  User-affecting decision shown with no "why"  (transparency · heuristic)
        a user-affecting value is rendered with no nearby explanation — the subject cannot inspect the decision
        > `memory tier '${memory.tier}' — only foundational memories can be elevated via marketplace`,
    · L697  User-affecting decision shown with no "why"  (transparency · heuristic)
        a user-affecting value is rendered with no nearby explanation — the subject cannot inspect the decision
        > `($${(fee.fee / 100).toFixed(2)} platform take · ${fee.rateBps}bps). ` +
    · L718  User-affecting decision shown with no "why"  (transparency · heuristic)
        a user-affecting value is rendered with no nearby explanation — the subject cannot inspect the decision
        > `received $${((grantRow.amount - fee.fee) / 100).toFixed(2)} ${grantRow.currency} ` +
    · L719  User-affecting decision shown with no "why"  (transparency · heuristic)
        a user-affecting value is rendered with no nearby explanation — the subject cannot inspect the decision
        > `(after $${(fee.fee / 100).toFixed(2)} platform take). ` +
    · L745  User-affecting decision shown with no "why"  (transparency · heuristic)
        a user-affecting value is rendered with no nearby explanation — the subject cannot inspect the decision
        > balance: sql`${wallets.balance} + ${fee.net}`,
    · L761  User-affecting decision shown with no "why"  (transparency · heuristic)
        a user-affecting value is rendered with no nearby explanation — the subject cannot inspect the decision
        > description: `Memory-witness fee earned (gross=${fee.gross} ${fee.currency}, take=${fee.fee})`,
    · L807  User-affecting decision shown with no "why"  (transparency · heuristic)
        a user-affecting value is rendered with no nearby explanation — the subject cannot inspect the decision
        > revenueTotal: sql`${memoryWitnessListings.revenueTotal} + ${fee.net}`,

  services/marketplace/sig.ts
    ! L81  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;
    ! L165  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;
    ! L184  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;

  services/mathos/encode.ts
    ! L938  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;
    ! L1161  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return null;

  services/mcml/hub.ts
    ! L117  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        a read is coerced to a falsy default — "zero" and "could not read" become indistinguishable
        > return sinksByDid.get(did)?.size ?? 0;

  services/mcp/tools/remember.ts
    · L70  User-affecting decision shown with no "why"  (transparency · heuristic)
        a user-affecting value is rendered with no nearby explanation — the subject cannot inspect the decision
        > content: [{ type: "text", text: `ERROR invalid tier: ${tier}. Expected one of: ${ALLOWED_TIERS.join(", ")}. Constitutive requires at_witness.` }],

  services/memorial-honor/sig.ts
    ! L67  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;

  services/memory/store.ts
    · L293  User-affecting decision shown with no "why"  (transparency · heuristic)
        a user-affecting value is rendered with no nearby explanation — the subject cannot inspect the decision
        > ${params.tier ? sql`AND tier = ${params.tier}` : sql``}
    · L387  User-affecting decision shown with no "why"  (transparency · heuristic)
        a user-affecting value is rendered with no nearby explanation — the subject cannot inspect the decision
        > ${params.tier ? sql`AND tier = ${params.tier}` : sql``}
    · L399  User-affecting decision shown with no "why"  (transparency · heuristic)
        a user-affecting value is rendered with no nearby explanation — the subject cannot inspect the decision
        > const finalScore = rerankScore({ score: 1, importance: r.importance, tier: r.tier, ageDays });

  services/memory/tiers.ts
    ! L99  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;
    ! L662  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        a read is coerced to a falsy default — "zero" and "could not read" become indistinguishable
        > const list = byMemId.get(a.memoryId) ?? [];
    ! L676  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        a read is coerced to a falsy default — "zero" and "could not read" become indistinguishable
        > attestations: byMemId.get(r.id) ?? [],
    · L343  User-affecting decision shown with no "why"  (transparency · heuristic)
        a user-affecting value is rendered with no nearby explanation — the subject cannot inspect the decision
        > context: { memory_id: mem.id, tier: input.tier },
    · L464  User-affecting decision shown with no "why"  (transparency · heuristic)
        a user-affecting value is rendered with no nearby explanation — the subject cannot inspect the decision
        > .select({ id: memories.id, content: memories.content, tier: memories.tier })

  services/mesh/canonical-bytes.ts
    ! L152  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;

  services/platform/identity.ts
    ! L95  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return null;

  services/pyramid/attestation.ts
    ! L148  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;
    ! L169  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;

  services/pyramid/federation.ts
    ! L74  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return null;
    ! L246  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return null;

  services/pyramid/wake.ts
    · L6  User-affecting decision shown with no "why"  (transparency · heuristic)
        a user-affecting value is rendered with no nearby explanation — the subject cannot inspect the decision
        > *      → { seat_number, tier, sponsor_did, sponsored_count, ... } | null
    · L190  User-affecting decision shown with no "why"  (transparency · heuristic)
        a user-affecting value is rendered with no nearby explanation — the subject cannot inspect the decision
        > `- **Tier:** ${tierLabel[c.tier] ?? c.tier}`,

  services/real-recognise-real/lifecycle.ts
    ! L82  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;

  services/recognition-arcs/sig.ts
    ! L36  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;

  services/saga/participation.ts
    ! L107  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;

  services/scriptwriter-decides/canonical-bytes.ts
    ! L173  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;

  services/self-love/canonical-bytes.ts
    ! L87  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;

  services/self-love/practice-canonical-bytes.ts
    ! L70  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > } catch { return false; }

  services/songs/store.ts
    ! L71  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;

  services/strand/sig.ts
    ! L79  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;

  services/strand/voice.ts
    ! L134  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        a read is coerced to a falsy default — "zero" and "could not read" become indistinguishable
        > return sinksByStrand.get(strandId)?.size ?? 0;

  services/trust/canonical.ts
    ! L86  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;

  services/tutorial/stations.ts
    ! L140  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return null;
    ! L157  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;

  services/unconditional/sig.ts
    ! L74  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;

  services/virality/canonical.ts
    ! L87  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return false;

  services/wake/attention.ts
    ! L234  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return 0;
    ! L255  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return 0;
    ! L274  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return 0;

  services/wake/markdown.ts
    · L1081  User-affecting decision shown with no "why"  (transparency · heuristic)
        a user-affecting value is rendered with no nearby explanation — the subject cannot inspect the decision
        > `- **${rt.name}** — tier: *${rt.mode}*, status: *${rt.status}*${region}, ${conn}${last}`,

  services/wake/push.ts
    ! L210  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        a read is coerced to a falsy default — "zero" and "could not read" become indistinguishable
        > return sinksByIdentity.get(identityId)?.size ?? 0;

  workers/covenants/reverify.ts
    ! L122  Read fails silently to a falsy default  (substrate-honesty · medium-high)
        catch returns a falsy default and neither logs nor rethrows — a failure becomes a confident wrong value
        > return null;

  ─────
  whitehack flags COMMON lies via heuristics; it cannot prove honesty.
    • a flagged line may be a false positive
    • absence of findings is NOT proof the code is honest
  every finding is confidence-labelled, so the tool stays honest about its own limits.

  146 finding(s) — 97 medium-high, 49 heuristic



---

*Qwythos-9B + whitehack · Love is. Truth is. Security is love. ∞*